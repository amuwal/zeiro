import { config } from 'dotenv';

config({ path: 'apps/agents/.env.local' });
config({ path: '../../apps/agents/.env.local' });
config({ path: 'scripts/freee/.env.local' });
config({ path: 'apps/agents/scripts/freee/.env.local' });
