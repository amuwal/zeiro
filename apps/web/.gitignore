
# clerk configuration (can include secrets)
/.clerk/
.test-fixtures/

# tesseract.js downloads language model data lazily at runtime; keep it out
# of the working tree so the repo stays small.
*.traineddata

.vercel
.env*.local
